package com.llm.connector.examples;

// Note: These annotations would be available in a Spring Boot project.
// This file is for demonstration purposes.

/*
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.beans.factory.annotation.Value;
import com.llm.connector.client.LlmClient;

@Configuration
public class LlmConfigExample {

    @Bean
    public LlmClient geminiClient(@Value("${gemini.api.key}") String apiKey) {
        return LlmClient.builder()
            .provider(LlmClient.ProviderType.GEMINI)
            .apiKey(apiKey)
            .model("gemini-pro")
            .build();
    }
    
    @Bean
    public LlmClient openAiClient(@Value("${openai.api.key}") String apiKey) {
        return LlmClient.builder()
            .provider(LlmClient.ProviderType.OPENAI)
            .apiKey(apiKey)
            .build();
    }
}
*/

public class SpringBootConfigExample {
    // This class is just a placeholder to show the commented code above
    // without requiring Spring dependencies in this project.
}
